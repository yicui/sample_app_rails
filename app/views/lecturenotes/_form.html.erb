<%= form_for(@lecturenote) do |f| %>
  <% if @lecturenote.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@lecturenote.errors.count, "error") %> prohibited this lecturenote from being saved:</h2>

      <ul>
      <% @lecturenote.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_field :content %>
  </div>
  <div class="field">
    <%= f.label :course_id %><br />
    <%= f.number_field :course_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
